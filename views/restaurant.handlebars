{{!-- <div>
	<h2>Single Restaurant: {{restaurant.name}}</h2>
	<img class="item-img" src="{{restaurant.image}}"/>
	<div class="dropdown">
  <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
    See All Menus
  </button>
  <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
    
        

            {{#each restaurant.Menus}} 
                <article >
                    <li><hr class="dropdown-divider"></li>
                     
                   <div class="btn-group dropend">
                     <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false"> {{title}} </button>      
                     
                     
                    </div>
                    
                </article>
        
            {{/each}}
  </ul>



    
    <a class="btn btn-primary" href="/restaurants" role="button">See All Restaurants List</a>
</div>

	<a class="btn" href="/restaurants">See Restaurant List</a>
</div> --}}

<h5 id="logo">{{restaurant.name}}</h5>
    <img src="{{restaurant.image}}" alt="..." width="1000" height="625"/><p>&nbsp;</p>
    <p><a href="/restaurants" class="btn btn-warning m-1">Back to
        Restaurants</a></p>
    <h2 class="mb-2 text-muted">Menu:</h2>

  {{#each restaurant.Menus}}
  <div class="col-md-3 justify-content-center offset-md-2">
    <h5>{{this.title}}</h5>
    <ul class="list-group list-group-flush">
      {{#each this.MenuItems}}
        <li class="list-group-item">{{this.name}} : ${{this.price}}</li>
      {{/each}}
    </ul>
  </div>
  {{/each}}